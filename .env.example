# Local development database.
# `npm run db:start` brings up a real PostgreSQL on this port; any Postgres works too.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/bioassetpro"
PORT=3001
